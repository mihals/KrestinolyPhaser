for( let i:number = 0; i<5; i++){
            this.add.image(134 + i*100,874,'backCard').setScale(0.8);
        }

        for( let i:number = 0; i<3; i++){
            this.add.image(236 + i*100,744,'backCard').setScale(0.8);
        }

        this.add.image(338,614,'backCard').setScale(0.8);